<%= form_for(@responder) do |f| %>
  <% if @responder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@responder.errors.count, "error") %> prohibited this responder from being saved:</h2>

      <ul>
      <% @responder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.number_field :capacity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
