<p id="notice"><%= notice %></p>

<h1>Listing Emergencies</h1>

<table>
  <thead>
    <tr>
      <th>Code</th>
      <th>Fire severity</th>
      <th>Police severity</th>
      <th>Medical severity</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @emergencies.each do |emergency| %>
      <tr>
        <td><%= emergency.code %></td>
        <td><%= emergency.fire_severity %></td>
        <td><%= emergency.police_severity %></td>
        <td><%= emergency.medical_severity %></td>
        <td><%= link_to 'Show', emergency %></td>
        <td><%= link_to 'Edit', edit_emergency_path(emergency) %></td>
        <td><%= link_to 'Destroy', emergency, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Emergency', new_emergency_path %>
